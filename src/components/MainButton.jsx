import React from "react";
import styles from "../styles/MainButton.module.css";

function MainButton() {
  return <button className={styles.mainButton}>Get Started</button>;
}

export default MainButton;
